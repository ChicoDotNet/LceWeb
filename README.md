# LceWeb

LCE web experiences and diagnostic backend.

This repository hosts the vanilla HTML landing experiences, the ASP.NET Core API that serves configurable diagnostics and captures leads, and Azure provisioning scripts based on PowerShell + Azure CLI.
